import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.khangnd.todoapp',
  appName: 'Capacitor Todo App',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
